import Engine from '../index';
import { BoardState } from '../../types';
/**
 * Returns all squares where pieces can be moved, for both white and black
 * @param boardState
 */
export default function availableMoves(this: Engine, boardState?: BoardState): void;
